#include <stdio.h>
/**
 * main - this is the main function
 *
 * Discription: this function prints all numbers digits
 *
 * Return: Always 0
 */
int main(void)
{
	int i;
	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");

	return (0);
}
