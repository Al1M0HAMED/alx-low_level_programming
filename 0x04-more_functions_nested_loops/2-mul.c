#include "main.h"

/**
 * mul - this function is for getting the multiplication for two intgars.
 *
 * @a: parametar is int.
 *
 * @b: parametar is int.
 *
 * Description: get two parametars and then return the value of multiplaction.
 *
 * Return: Always the value of the multiplaction between two parametars.
 */
int mul(int a, int b)
{
	return (a * b);
}
