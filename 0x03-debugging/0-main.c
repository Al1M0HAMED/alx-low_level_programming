#include "main.h"
#include <stdio.h>
/**
 * main - this function tests if the function positive_or_negative works.
 *
 * Return: Always 0.
 */
int main(void)
{
	postitive_or_negative(0);
	return (0);
}
