#include "main.h"
#include <stdio.h>
/**
 * main - this function tests if the function positive_or_negative works.
 *
 * Return: Always 0.
 */
int main(void)
{
	int i;
	i = 0;

	positive_or_negative(i);

	return (0);
}
